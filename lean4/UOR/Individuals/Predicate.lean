-- @generated by uor-lean from uor-ontology — do not edit manually
--
-- UOR Foundation — Predicate individual constants.

import UOR.Structures
import UOR.Individuals.State

open UOR.Primitives

namespace UOR.Kernel.Predicate

-- True on every Datum. Match-arm default catch-all.
def always : UOR.Kernel.Predicate.Predicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/schema/Datum" : String))
  boundedEvaluator := #[]
}

-- True iff the FreeRank deficit is zero.
def budgetExhausted : UOR.Kernel.Predicate.StatePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/partition/FreeRank" : String))
  boundedEvaluator := #[]
}

-- True iff the resolver has entered a ContradictionBoundary.
def contradictionReached : UOR.Kernel.Predicate.StatePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/state/ContradictionBoundary" : String))
  boundedEvaluator := #[]
}

-- True iff the Datum parity bit is 0.
def isEven : UOR.Kernel.Predicate.TypePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/schema/Datum" : String))
  boundedEvaluator := #[]
}

-- True iff op(op(x)) = x for the bound op.
def isInvolution : UOR.Kernel.Predicate.TypePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/schema/Datum" : String))
  boundedEvaluator := #[]
}

-- True iff the Datum parity bit is 1.
def isOdd : UOR.Kernel.Predicate.TypePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/schema/Datum" : String))
  boundedEvaluator := #[]
}

-- True iff the Datum is the multiplicative identity.
def isUnit : UOR.Kernel.Predicate.TypePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/schema/Datum" : String))
  boundedEvaluator := #[]
}

-- True iff the Datum is the additive identity of its ring.
def isZero : UOR.Kernel.Predicate.TypePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/schema/Datum" : String))
  boundedEvaluator := #[]
}

-- False on every Datum. Disabled-arm marker.
def never : UOR.Kernel.Predicate.Predicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/schema/Datum" : String))
  boundedEvaluator := #[]
}

-- True iff the reduction fixpoint has been reached.
def reductionConverged : UOR.Kernel.Predicate.StatePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/reduction/ReductionState" : String))
  boundedEvaluator := #[]
}

-- True iff the named site coordinate is currently free.
def siteFree : UOR.Kernel.Predicate.SitePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/partition/SiteIndex" : String))
  boundedEvaluator := #[]
}

-- True iff the named site coordinate is currently pinned.
def sitePinned : UOR.Kernel.Predicate.SitePredicate UOR.Prims.Standard := {
  evaluatesOver := some (("https://uor.foundation/partition/SiteIndex" : String))
  boundedEvaluator := #[]
}

end UOR.Kernel.Predicate
